"""Pure Gmail operations: list, fetch, attachments.

Filled in during Step 4. Each operation takes a user_id and resolves
the service via :func:`agenticfiller.gmail.client.get_gmail_service`.
"""

from __future__ import annotations

# To be implemented in Step 4:
# - list_emails(user_id, query, max_results)
# - get_email(user_id, message_id)
# - list_pdf_attachments(user_id, message_id)
# - download_pdf(user_id, message_id, attachment_id, filename)
